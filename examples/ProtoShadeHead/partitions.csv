# Partition table for ProtoShadeHead on an 8 MB ESP32-S3.
#
# Arduino IDE:   put this file next to the .ino and pick Tools > Partition Scheme > Custom.
# arduino-cli:   --build-property build.partitions=partitions --build-property
#                upload.maximum_size=3342336
#
# Two partitions matter here:
#
#   protoshade   the .bin the editor produces. A data partition, so esp_partition_mmap()
#                can map it into the address space and the packed images cost no RAM.
#                1 MB holds a program with a few 512x512 images in it; make it bigger if
#                you pack more, smaller if your flash is 4 MB.
#   littlefs     the editor itself (npm run build:device), gzipped. Drop this partition and
#                shrink nothing else if you only ever use the built-in /upload page.
#
# Offsets must stay 4 KB aligned and app partitions 64 KB aligned. Sizes are hex bytes.
#
# name,      type, subtype, offset,   size,     flags
nvs,         data, nvs,     0x9000,   0x5000,
phy_init,    data, phy,     0xE000,   0x1000,
factory,     app,  factory, 0x10000,  0x330000,
protoshade,  data, 0x40,    0x340000, 0x100000,
littlefs,    data, spiffs,  0x440000, 0x3C0000,
